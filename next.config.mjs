/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.glamour.de', 'static.wikia.nocookie.net', 'i.pinimg.com', 'uwagg6ytf2u3hnso.public.blob.vercel-storage.com'],
    }
};

export default nextConfig;
